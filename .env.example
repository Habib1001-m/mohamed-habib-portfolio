# This is a client-only static portfolio. No environment variables or secrets are required.
# All data processing, interactive WebGL, and contact form operations are handled entirely on the client side.
